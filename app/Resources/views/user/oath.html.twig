{% extends 'base.html.twig' %}

{% block body %}
    <h1>Generate code </h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Generate" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('fos_user_profile_show') }}">Back to profile</a>
        </li>
    </ul>
{% endblock %}
