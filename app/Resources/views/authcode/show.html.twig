{% extends 'base.html.twig' %}

{% block body %}
    <h1>Authcode</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ authCode.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('authcode_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('authcode_edit', { 'id': authCode.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
